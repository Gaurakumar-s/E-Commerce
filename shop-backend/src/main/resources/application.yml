spring:
  application:
    name: shop-backend

  datasource:
    url: jdbc:mysql://localhost:3306/shop_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: shopuser
    password: StrongPass123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: equinexalerts@gmail.com
    password: sxwknegmhmavjvgn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  secret: THIS_IS_A_SUPER_LONG_SECURE_JWT_SECRET_KEY_256_BITS_MINIMUM_CHANGE_IN_PRODUCTION_123456789
  expiration-ms: 3600000
